import 'package:flutter_test/flutter_test.dart';
import 'package:picsss/main.dart';

void main() {
  test('PicsssApp can be constructed', () {
    expect(const PicsssApp(initialPath: null), isA<PicsssApp>());
  });
}
